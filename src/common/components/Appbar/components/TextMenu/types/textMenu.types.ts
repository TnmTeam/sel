export type TextMenuType = {
    url: string;
    currentRoute: string;
    title: string;
    buttonType: string;
};

export type StudentCourseItemType = {
    student: string;
    course: string[];
};