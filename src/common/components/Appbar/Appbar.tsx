import { Colors } from "@/common/themes/Color";
import { Stack } from "@mui/material";

export const Appbar = () => {
  return (
    <Stack sx={{ height: "89px", backgroundColor: Colors.BackBlue }}></Stack>
  );
};
