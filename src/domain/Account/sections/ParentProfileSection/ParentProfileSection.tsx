import { Stack } from '@mui/system';
import { ParentProfile } from './components';

export const ParentProfileSection = () => {
    return (
        <Stack>
            <ParentProfile />
        </Stack>
    );
};
