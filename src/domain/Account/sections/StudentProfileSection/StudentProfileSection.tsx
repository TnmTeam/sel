import { Stack } from '@mui/system';
import { StudentProfile } from './components';

export const StudentProfileSection = () => {
    return (
        <Stack>
            <StudentProfile />
        </Stack>
    );
};
