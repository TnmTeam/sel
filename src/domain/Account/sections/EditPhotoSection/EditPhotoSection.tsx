import { Stack } from '@mui/system';
import { PhotoPopup } from './components';

export const EditPhotoSection = () => {
    return (
        <Stack>
            <PhotoPopup />
        </Stack>
    );
};
