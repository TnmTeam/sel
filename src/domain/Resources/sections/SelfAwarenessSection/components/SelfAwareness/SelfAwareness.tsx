import { css } from '@emotion/react';
import { Stack, Typography } from '@mui/material';

export const SelfAwareness = () => {
    return (
        <Stack css={sx.contentContainer}>
            <Typography variant='h1'>SelfAwareness</Typography>
        </Stack>
    );
};

const sx = {
    contentContainer: css``,
};
